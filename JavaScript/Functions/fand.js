function addFandC(){ //para evitar aparecer depois na Camara ortografica
    cena.add(ceiling);
    cena.add(Sky);
    cena.add(ceiling2);
}

function removeFandC(){ //para evitar aparecer depois na Camara ortografica
    cena.remove(ceiling);
    cena.remove(Sky);
    cena.remove(ceiling2);
}
